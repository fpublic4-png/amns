import 'package:flutter/material.dart';

class NotifyTeachersPage extends StatelessWidget {
  const NotifyTeachersPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Notify Teachers')),
      body: const Center(child: Text('Notify Teachers Page')),
    );
  }
}
