import 'package:flutter/material.dart';

class NotifyStudentsPage extends StatelessWidget {
  const NotifyStudentsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Notify Students')),
      body: const Center(child: Text('Notify Students Page')),
    );
  }
}
